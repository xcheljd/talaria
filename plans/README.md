# Implementation Plans

Generated by the improve skill on 2026-06-17, against commit `c469626`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

All plans are read-only on this index until you execute them. Every plan stamps
`Planned at: c469626` and starts with a drift check — if in-scope files changed
since then, reconcile before proceeding.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 001 | Global drop guard — file dropped outside the zone no longer replaces the app (+ out-of-zone hint toast) | P1 | S | LOW | — | DONE |
| 002 | `saveBlob` unit tests (Tauri + browser branches) | P1 | S | LOW | — | DONE |
| 003 | `read_file_as_data_url` path scoping + honest comment | P1 | S | LOW | — | DONE |
| 004 | Standardize dev port on 5173 + run E2E suite in CI | P1 | M | LOW | — | DONE (merged to main as `715b5f5`. Port aligned to 5173, stale export-dialog spec + flaky webkit palette tests repaired, e2e CI job added. Verified on branch: typecheck/lint clean, unit 982/982, e2e 99/99.) |
| 005 | Warn on failed PDF restore instead of silently dropping attachments | P2 | M | LOW | — | DONE (executor: branch `advisor/005-pdf-hydration-warning`, commit `d3f227d`, NOT merged. Reviewed & verified: typecheck/lint clean, store tests 126/126, full suite 986/986. Flag confirmed non-persisted.) |
| 006 | Validate TipTap `data-color` before inlining into `style` | P2 | S | LOW | — | DONE (executor: droid `--auto high`, branch `advisor/006-data-color-validation`, commit `4c65874`, NOT merged. Reviewed & verified: typecheck/lint clean, module 15/15, full suite 990/990. Injection test confirms `background-image` payload dropped → `yellow` fallback.) |
| 007 | PDFAttachments drag-and-drop tests (direct + `file://` URI) | P2 | M | LOW | 001 (soft) | DONE (executor: droid `--auto high`, branch `advisor/007-008-batch`, commit `efa2716`, NOT merged. Reviewed & verified: 7 tests cover direct-file/uri-list/Windows-path/html-fallback/de-dupe/invoke-error; `PDFAttachments.tsx` unchanged; full suite 1007/1007.) |
| 008 | `db.ts` error/resilience branch tests | P2 | M | MED | — | DONE (executor: droid `--auto high`, branch `advisor/007-008-batch`, commit `7dd47f2`, NOT merged. Reviewed & verified: 10 chars. tests (request-error/orphan-keys/round-trip); `db.ts` unchanged. NOTE: corrected plan's stale claim — `getPDFFromIndexedDB` resolves null on error, not rejects; plan 005 warning still fires either way.) |
| 009 | `dedup_objects` merges only on identical serialized bytes (not 64-bit hash) | P2 | S | LOW | — | DONE (executor: worktree `agent-ab5534d65b790df1c`, branch `advisor/009-dedup-byte-comparison`, commit `83582e7`. Verified: 18/18 tests pass, 0 clippy warnings, scope clean — only `amatl.rs` changed. `hash_bytes` deleted, import removed, map keyed on `Vec<u8>`, 2 new dedup tests assert merge/no-merge behavior.) |
| 010 | Optimizer fail-safe contract holds on panics (`catch_unwind` around `try_optimize`) | P2 | S | LOW | — | DONE (executor: worktree `agent-ad43889fa787db35f`, branch `advisor/010-panic-safe-optimizer`, commit `f4c18e4`. Verified: 17/17 tests pass, 0 clippy warnings, scope clean. `catch_unwind` at line 95, `Ok(Ok(out))` double-unwrap correct, no `AssertUnwindSafe` needed. New test `corrupt_jpeg_stream_falls_back_without_crashing` passes. Merge note: worktree based on 63e4c47 not 83582e7 — use `git merge` not `--ff-only`.) |
| 011 | Fix stale `pack_and_save` doc comment ("stub … does not pack" → it does, app ships it) | P3 | S | LOW | — | DONE (executor: this session, comment-only edit to `amatl.rs:549-560`. Verified: 19/19 tests pass, 0 clippy warnings. Drift check confirmed file changed since 63e4c47 but the doc comment + body matched plan excerpts exactly. No new tests needed — behavior unchanged.) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Second audit pass (2026-06-20, commit `63e4c47`)

Plans 009–011 come from a second `improve` run focused on the **Rust backend**
(`src-tauri/src/amatl.rs` + `lib.rs`), which is new since the first audit
(commit `c469626`) — the amatl PDF optimizer, accessibility strip, and
object-stream packing all landed afterward and were unaudited. The first audit's
TypeScript findings (001–008, all DONE) were not re-examined.

Plans 009–011 are independent of each other and of 001–008. If executing 009 and
010 together, do 009 first (both touch `amatl.rs`, different functions — ordering
keeps diffs clean). All three are S-effort, LOW-risk, verified by
`cargo test --manifest-path src-tauri/Cargo.toml` and
`cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`
(first build needs NASM + cmake + a C compiler for mozjpeg).

### Direction findings from this pass (not planned — maintainer's call)

- **Extract `amatl` to a standalone crate.** The code is already library-shaped
  (`OptimizeOptions`, library-neutral naming, MIT/BSD-only deps) and AGENTS.md
  states the intent. Worth a spike once a second consumer exists; fix 009 + 010
  first (don't publish a crate that can panic on untrusted input). Coarse effort: M.
- **Expose accessibility-strip as a user setting.** `strip_accessibility` is
  plumbed end-to-end through the IPC but hardcoded `true` in the TS wrapper
  (`pdf-utils.ts:77`); AGENTS.md says to revisit if the audience shifts. Coarse
  effort: S–M.

### Findings considered and rejected (second pass)

- **`gh aw` agentic workflows** (`.github/workflows/agentics-maintenance.yml`,
  `simplify-and-harden-ci.*`) — not a finding. Generated by GitHub Agentic
  Workflows: SHA-pinned actions, scoped per-job permissions, `check_team_member`
  gating, and fork guards. Follows current hardening practice.
- **`add_xref_self_entry` lopdf 0.41 workaround** (`amatl.rs:580-673`) — already
  documented and tracked for removal once lopdf is bumped past 0.41 (the upstream
  fix, J-F-Liu/lopdf#501, isn't in a published release yet). Blocked on the dep
  bump, not on us.
- **Form-XObject images not downsampled** — `collect_placements` only walks page
  content streams, so images painted inside Form XObjects are never measured and
  are left untouched. This is a *safe* missed-optimization (fail-safe by design:
  "images we can't measure a placement for are left untouched"), not a bug.
  M+ effort to add recursion, input-dependent gain. Not worth it now.

## Recommended sequencing

The P1 set (001–004) is the highest-leverage, lowest-risk batch and has no
inter-dependencies — do those first, in any order. Suggested order is by user
priority (001 is the reported bug) then by blast radius.

## Execution log

- **001 — DONE (rounded).** Two stacked commits on branch
  `advisor/001-drop-guard-rounded` (worktree
  `.claude/worktrees/agent-ad4e065e978dcada1`):
  `e0286bf` adds the `useGlobalDropGuard` hook + mount + tests;
  `f73ce53` rounds it out — when a file is dropped outside any registered drop
  zone (detected via `event.defaultPrevented`, so no coupling to the drop zone
  or editor), it shows a single deduped hint toast in addition to preventing
  navigation. Verified: typecheck 0, lint 0, full suite 972/972. **Merge this
  branch** (it supersedes the standalone `advisor/001-global-drop-guard`).
  Reviewer-confirmed the toast does not fire on valid drops (PDF zone, editor
  image drops both call `preventDefault`).

## Dependency notes

- **007 soft-depends on 001**: Plan 001 adds a window-level drop guard but does
  not modify `PDFAttachments.tsx`, so 007 can run independently. Doing 001 first
  means 007 tests the component in its final runtime context. Not a hard block.
- **005 and 008 share a contract** (resolved 2026-06-19): on a read error
  `getPDFFromIndexedDB` **resolves `null`** (it swallows; it does not reject).
  005's load-warning fires correctly under this contract anyway — a `null`
  result is omitted from `restoredPDFs`, so the count mismatch trips the
  warning. 008 characterizes the resolves-null behavior; both plans are DONE
  and agree.
- Otherwise all plans are independent.

## What was audited (and what was not)

`standard` audit, hotspot-weighted across all nine categories via four parallel
read-only agents, then every reported finding was re-read and vetted against the
live code. Covered: the promotion store/persistence, EML/email-HTML generation,
import/export validation, the Tauri Rust backend, sanitization, file-save, the
PDF attachment flow, tests/CI/deps/docs, and direction.

Not deeply audited: the TipTap newsletter editor internals
(`NewsletterToolbar`/`NewsletterStylePanel`, ~1300 lines combined) beyond their
data flow into generation; the shadcn `ui/` primitives; and runtime/perf under
real large-newsletter load (judged a non-issue — see rejected findings).

## Findings considered and rejected

(So nobody re-audits them.)

- **"Email HTML regenerates on every keystroke" (perf)** — rejected. The
  page-level `emailHTML` memo (`PromotionPage.tsx:87`) subscribes via `useShallow`
  to exactly `selectEmailDataSource` (email-affecting fields) plus two stable fn
  refs; collapse/bulk-recipient changes do not trigger regeneration. Already
  correctly memoized; the code comment documents it.
- **"Unawaited `saveToIndexedDB` loses errors" (correctness)** — downgraded /
  not worth a plan. `saveToIndexedDB` catches internally and drives the existing
  global `saveStatus` warning toast, so the missing `await` costs essentially
  nothing.
- **"iframe `allow-same-origin` enables exfiltration" (security)** — rejected.
  The preview iframe has no `allow-scripts`, so framed content cannot execute JS
  regardless; combined with the strict CSP, this is not a vulnerability.
- **"`@tiptap/extension-blockquote` is a dead dependency" (tech-debt)** —
  rejected. Blockquote is actively used (`NewsletterToolbar.tsx:404`,
  `extensions.ts:92`); at most the explicit package duplicates StarterKit. Not
  worth churn.
- **`generateId` collision (correctness)** — not planned. Requires >1000 ID
  generations within a single millisecond; only plausibly reachable via a huge
  synchronous config import. Narrow; the author already reasoned about the
  wrap-around in a comment. Revisit only if bulk import grows.
- **God-module / newsletter-duplication refactors (tech-debt)** — not planned.
  L-effort, HIGH-risk refactors across 40+ consumers; the team already did
  targeted split passes. Low leverage vs. risk right now.
- **TipTap minor lag 3.22→3.26, no pre-commit hooks (deps/dx)** — noted, not
  planned. Hygiene-only; CI already enforces the gates a pre-commit hook would.

## Direction findings (not planned — maintainer's call)

- ~~**Discoverable "Load saved promotion" entry point.**~~ **RETIRED 2026-06-19
  — audit miss.** This is already shipped: an "Import" button (Upload icon,
  `aria-label="Import"`) renders in the Email Preview header bar beside Export
  and Start Over (`PreviewColumn.tsx:453-462`), wired to `handleImportConfig` →
  `validateImportConfig`. The feature is exposed exactly where this finding
  proposed putting it; nothing to do.
- **Batch preview before bulk send.** `computeEmailStats()` already yields the
  data; a "which recipients land in which batch" preview is one component away.
  Ask for a spike plan if wanted.
