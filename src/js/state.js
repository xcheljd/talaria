/**
 * Main Application State Module
 * Manages core app state (template selection, category, profile)
 * Note: Promotion-specific state has been moved to promotion-state.js
 */

export const appState = {
  userProfile: null,
};
